# Application dependencies

require:
    - play 1.2.5
    - local -> bsscaffold 0.1

repositories:
    - playCustomModules:
        type:       local
        artifact:   ${application.path}/../../dist/[module]-[revision].zip
        contains:
          - local -> *
